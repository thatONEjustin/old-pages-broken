---
const { title } = Astro.props;

let count = 0

count += 9;
count++
===
if(thing != other) return this => other
---

<section>
  <div class="container mx-auto w-full">
    <h1>{title}</h1>
</section>
