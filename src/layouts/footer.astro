<!-- :class="baseThemeClass" -->
<footer class="Content--dark"> 
    <ul class="Footer-tech">
        <li class="pr-3 border-r border-yellow-500">
            <span class="font-semibold">hosted on</span> <a href="https://pages.github.com/" target="_blank">GitHub Pages</a>
        </li>
        <!-- <li class="pl-2 mr-2 border-l"></li> -->
        <!-- {% for tech in site.data.madeWith %}
        <li class="mr-1 {% if forloop.index == 1 %}pl-3{% endif %}">
            {% if forloop.index == 1 %}
            <span class="font-semibold">made with:</span>
            {% endif %}
            <a href="{{ tech.url }}" target="_blank">
                {{ tech.name }}{% if forloop.index != forloop.length %},{% endif %}
            </a>
        </li>
        {% endfor %} -->
    </ul>
    <div>
        <!-- <ul class="ThemeSelector">
            <li class="ThemeSelector-label">palette: </li>
            <template x-for="choice in palettes">
            <li>
                <a
                    class="ThemeSelector-choice"
                    @click.prevent.debounce="choose(choice.name)"
                    :data-theme="`${choice.name}`"
                    :class="choicePreview(choice.name)"></a>
            </li>
            </template>
        </ul> -->
        <p><span>copyright</span> 2023</p>
    </div>
</footer>

<style>
    footer {
        @apply
            flex
            items-center
            justify-between
            w-full
            px-4
            py-3
            bg-gradient-to-tr
            from-blue-950
            to-blue-900
            border-t
            border-blue-950
            bottom-0
            mt-auto
            fixed
            flex-col
            h-[80px];
    }

    ul {
        @apply
            flex
            flex-row;
    }

    ul li {
        @apply text-xs; 
    }

    ul li a {
        @apply text-xs
            italic;
    }

    ul li, li a {
        @apply text-yellow-500;
    }

    footer div {
        @apply flex
            flex-row
            justify-between
            items-center
            w-min;
    }

    p {
        @apply text-yellow-500
            text-sm
            flex;
    }

    p span {
        @apply font-semibold
            pr-1;
    }

    /* 
    @screen sm {
        footer {
            @apply relative
                bottom-[unset];
        }
    }
    */

    @screen lg {
        footer {
            @apply flex-row
                h-[40px];
        }
    }
</style>